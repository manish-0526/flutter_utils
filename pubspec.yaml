name: flutter_utils
description: "Flutter package for commonly used utils."
version: 0.0.1
homepage: null

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  cross_file: ^0.3.3+8
  file_picker: ^8.1.7
  flutter:
    sdk: flutter
  intl: ^0.20.2
  mime: ^1.0.5

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
